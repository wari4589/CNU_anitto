# HTML Legacy

Original static MANITTO files, kept for archive only.

Files:

- `index.html`
- `style.css`
- `app.js`
- `supabase.js`

Supabase URL/key removed from `supabase.js`. Legacy admin password removed from `app.js`.

Use current Next.js version in repo root for real deploy.
